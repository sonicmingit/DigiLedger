<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.digiledger.backend.mapper.DictTagMapper">

    <resultMap id="tagResultMap" type="com.digiledger.backend.model.entity.DictTag">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="parentId" column="parent_id" />
        <result property="color" column="color" />
        <result property="icon" column="icon" />
        <result property="sort" column="sort" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <sql id="baseColumns">
        id, name, parent_id, color, icon, sort, created_at, updated_at
    </sql>

    <select id="findAll" resultMap="tagResultMap">
        SELECT <include refid="baseColumns"/>
        FROM dict_tag
        ORDER BY sort ASC, id ASC
    </select>

    <select id="findById" parameterType="long" resultMap="tagResultMap">
        SELECT <include refid="baseColumns"/> FROM dict_tag WHERE id = #{id}
    </select>

    <select id="findByIds" resultMap="tagResultMap">
        SELECT <include refid="baseColumns"/>
        FROM dict_tag
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="findByAssetId" resultMap="tagResultMap">
        SELECT t.id, t.name, t.parent_id, t.color, t.icon, t.sort, t.created_at, t.updated_at
        FROM dict_tag t
        INNER JOIN asset_tag_map atm ON atm.tag_id = t.id
        WHERE atm.asset_id = #{assetId}
        ORDER BY t.sort ASC, t.id ASC
    </select>

    <insert id="insert" parameterType="com.digiledger.backend.model.entity.DictTag" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dict_tag (name, parent_id, color, icon, sort)
        VALUES (#{name}, #{parentId}, #{color}, #{icon}, #{sort})
    </insert>

    <update id="update" parameterType="com.digiledger.backend.model.entity.DictTag">
        UPDATE dict_tag
        SET name = #{name},
            parent_id = #{parentId},
            color = #{color},
            icon = #{icon},
            sort = #{sort},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM dict_tag WHERE id = #{id}
    </delete>

    <select id="countChildren" resultType="long">
        SELECT COUNT(1) FROM dict_tag WHERE parent_id = #{parentId}
    </select>

    <select id="countByNameAndParent" resultType="long">
        SELECT COUNT(1) FROM dict_tag
        WHERE name = #{name}
          AND (parent_id <=> #{parentId})
        <if test="excludeId != null">
            AND id <> #{excludeId}
        </if>
    </select>

</mapper>
