<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.digiledger.backend.mapper.EquipUpgradeLinkMapper">

    <resultMap id="linkResultMap" type="com.digiledger.backend.model.entity.EquipUpgradeLink">
        <id column="id" property="id" />
        <result column="route_id" property="routeId" />
        <result column="from_node_id" property="fromNodeId" />
        <result column="to_node_id" property="toNodeId" />
        <result column="remark" property="remark" />
        <result column="is_deleted" property="isDeleted" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="findByRouteId" parameterType="long" resultMap="linkResultMap">
        SELECT *
        FROM equip_upgrade_link
        WHERE route_id = #{routeId} AND is_deleted = 0
        ORDER BY id ASC
    </select>

    <select id="findById" parameterType="long" resultMap="linkResultMap">
        SELECT *
        FROM equip_upgrade_link
        WHERE id = #{id} AND is_deleted = 0
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.digiledger.backend.model.entity.EquipUpgradeLink" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO equip_upgrade_link (route_id, from_node_id, to_node_id, remark)
        VALUES (#{routeId}, #{fromNodeId}, #{toNodeId}, #{remark})
    </insert>

    <update id="softDelete" parameterType="long">
        UPDATE equip_upgrade_link
        SET is_deleted = 1,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="softDeleteByNodeId" parameterType="long">
        UPDATE equip_upgrade_link
        SET is_deleted = 1,
            updated_at = CURRENT_TIMESTAMP
        WHERE from_node_id = #{nodeId} OR to_node_id = #{nodeId}
    </update>

    <update id="softDeleteByRoute" parameterType="long">
        UPDATE equip_upgrade_link
        SET is_deleted = 1,
            updated_at = CURRENT_TIMESTAMP
        WHERE route_id = #{routeId}
    </update>
</mapper>
